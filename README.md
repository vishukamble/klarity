# klarity
One command. Every cluster. Everything that's wrong. klarity is a read-only Kubernetes diagnostic CLI that scans multiple clusters in parallel, classifies unhealthy workloads by root cause (OOM, CrashLoopBackOff, image pull errors, pending pods, HPA ceiling), and renders categorized terminal tables with one-line error summaries.
